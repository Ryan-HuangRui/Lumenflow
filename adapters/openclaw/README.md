# OpenClaw Adapter

This adapter will document how to expose the portable photo skills to OpenClaw.

Keep platform-specific setup here. The shared workflow should continue to live in `skills/`, `knowledge/`, and `scripts/`.
